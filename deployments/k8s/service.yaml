apiVersion: v1
kind: Service
metadata:
  name: loan-repayment-calculator
spec:
  selector:
    app: loan-repayment-calculator
  #type: LoadBalancer
  ports:
    - protocol: TCP
      name: grpc-port
      port: 50051
      targetPort: 50051
    - protocol: TCP
      name: http-port
      port: 50052
      targetPort: 50052
